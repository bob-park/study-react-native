import { Text, View } from 'react-native';

export default function Search() {
  return (
    <View className="flex size-full flex-col items-center justify-center gap-2 dark:bg-black">
      <Text className="dark:text-white">search</Text>
    </View>
  );
}
